<div class="row">
  <div class="span8">
    <h1>Partits</h1>
    <table class="table table-striped table-bordered">
      <thead>
        <tr>
          <th>Accio</th>
          <th>Partit</th>
          <th>Aposta</th>
          <th>Hora Partit</th>
          <th>Tancament</th>
          <th>Boxing</th>
        </tr>
      </thead>
      <tbody>
        <% @games.each do |game| %>
        <tr>
          <td>
            <% if game.closeTime.past? %>
            <%= link_to "Tancat",
            game_bets_path(game.id),
            :class => 'btn btn-primary' %>
            <% else %>
            <% if game.bets.where(user_id: current_user.id).exists? %>
            <%= link_to "Cambiar",
            edit_game_bet_path(game.id,game.bets.where(user_id: current_user.id).take(1)[0].id),
            :class => 'btn btn-primary' %>
            <% else %>
            <%= link_to "Jugar",
            new_game_bet_path(game.id),
            :class => 'btn btn-primary' %>
            <% end %>
            <% end %>  
          </td>
          <td><%= game.description %></td>
          <td>
            <% if game.userbet(current_user) %>
            <%= game.userbet(current_user).score1 %>
            <% if game.numscore >= 2 %>
            - <%= game.userbet(current_user).score2 %>
            <% end %>
            <% if game.numscore >= 3 %>
            - <%= game.userbet(current_user).score3 %>
            <% end %>

            <% end %>  
          </td>
          <td><%= game.gameTime %></td>
          <td><%= game.closeTime %></td>
          <td>
            <% if game.boxing %>
            <%= link_to game.boxing.description,
            standing_boxing_path(game.boxing.id),
            :class => 'btn btn-info' %>
            <% end %>
          </td>  


        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="span4">
    <h3>Classificacio</h3>
    <table class="table table-striped table-condensed table-bordered">
      <thead>
        <tr>
          <th>Jugador</th>
          <th>Punts</th>
        </tr>
      </thead>
      <tbody>
        <% @standings.each do |user| %>
        <tr>
          <td><%= user.username %></td>
          <td><%= user.numpoints %></td>
        </tr>
        <% end %>

      </tbody>
    </table>
  </div>
</div>
